# .env 文件内容示例
# PostgreSQL 配置
POSTGRES_USER_VAR=focalboard_user
POSTGRES_PASSWORD_VAR=your_secret_password_here # 请修改为强密码
POSTGRES_DB_VAR=focalboard_db

# Focalboard 主机端口 (可选, 默认 8000)
FOCALBOARD_HOST_PORT=8000