# .env file
# PostgreSQL 数据库配置
POSTGRES_DB=freshrss
POSTGRES_USER=freshrss_user
POSTGRES_PASSWORD=your_strong_password

# FreshRSS 服务的对外端口
FRESHRSS_PORT=8080

# Watchtower 更新检查间隔 (秒)
WATCHTOWER_INTERVAL=3600

# 设置时区, 例如 Asia/Shanghai, Europe/Berlin, America/New_York
TZ=Asia/Shanghai